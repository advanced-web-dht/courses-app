import styled from 'styled-components';

const PopupBox = styled.div`
	display: flex;
	align-items: center;
	padding: 15px 25px;
	.MuiTextField-root {
		margin-right: 10px;
		width: 250px;
	}
`;

export default PopupBox;
